# .NET
bin/
obj/
*.user
*.suo
.vs/
*.DotSettings.user
TestResults/

# EF Core migrations (keep Migrations/ folder, ignore snapshots in CI)
# Note: DO commit migration files

# Angular
frontend/node_modules/
frontend/dist/
frontend/.angular/

# Generated API client — api.ts (hand-written) and api/ (openapi-generator output) are both committed
frontend/src/app/generated/api/.openapi-generator/
frontend/src/app/generated/api/git_push.sh

# Environment / secrets
appsettings.*.json
!appsettings.json
!appsettings.Development.json
!appsettings.Test.json
.env
.env.local

# Local Docker Compose overrides
docker-compose.override.yml

# macOS
.DS_Store

# JetBrains / VS Code
.idea/
.vscode/
*.swp

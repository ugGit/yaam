import { Routes } from '@angular/router';
import { RemindersListComponent } from './pages/reminders-list/reminders-list.component';

export const REMINDERS_ROUTES: Routes = [{ path: '', component: RemindersListComponent }];
